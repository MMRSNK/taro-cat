# Canned reply images

Drop your own images here for the pre-canned replies used on suspicious mentions.

- Reference them from `prompts/canned_replies.yaml` (e.g. `image: assets/canned/1.png`).
- Any size/format Pillow reads is fine; PNG/JPG recommended.
- If a referenced image is missing, the bot just posts the text (no image).

# Secrets — NEVER commit
.env
token.json
credentials.json

# Scratch / generated
.tmp/
.tmp_rules.txt
data/state.json
data/prompts.log

# cards/ ARE tracked (downscaled deck, ~600px) so deploy is a plain git pull.
# 2K archive stays in images-final-hq/ (ignored below).

# Large deck image working folders (raw scans, crops, HQ output)
images-raw/
images-final/
images-final-hq/

# Firebase AI (Node upscaler) — config holds the API key, never commit
tools/upscale/node_modules/
tools/upscale/firebase-config.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# OS
.DS_Store
Thumbs.db
